<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form_for(user) do |f| %>
    <div class="actions">
        <p><%= f.text_field :fname %><%= f.label :fname %></p>
        <p><%= f.text_field :lname %><%= f.label :lname %></p>
        <p><%= f.text_field :email %><%= f.label :email %></p>
        <p><%= f.text_field :username %><%= f.label :username %></p>
        <p><%= f.text_field :password %><%= f.label :password %></p>
        <%= f.submit %>
    </div>
    <% end %>

  </div>
<% end %>
